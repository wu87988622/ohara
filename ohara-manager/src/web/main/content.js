oharaManager.widget.content = {
    clean: function() {
      this.$baseEl.empty();
    },
    append: function(panel) {
      this.$baseEl.append(panel);
    }
};

oharaManager.widget.contentHeader = {
    clean: function() {
      this.$baseEl.empty();
    },
    append: function(panel) {
      this.$baseEl.append(panel);
    }
};